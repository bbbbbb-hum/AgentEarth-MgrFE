version: '3.8'

services:
  web:
    build:
      context: ./dist
      dockerfile: ../Dockerfile
    container_name: ae-mgr-fe
    ports:
      - "9014:80"
    restart: always

networks:
  default:
    name: ae-mgr-net
